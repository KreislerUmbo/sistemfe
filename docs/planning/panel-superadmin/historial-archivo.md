# Historial archivado — Panel Superadmin (gestión central de tenants)

> Archivo creado el 20-ago-2026 al recortar `plan-panel-superadmin.md` (1518
> líneas) a un stub corto — todo su contenido estaba genuinamente cerrado,
> sin ninguna decisión de diseño pendiente salvo las 2-3 anotadas en el
> stub. Reconstruido comprimiendo el propio documento original (que ya
> traía hallazgos/bugs reales documentados fase por fase, verificados
> contra `sandbox`/tenants descartables reales, no solo diseño teórico) —
> no es un volcado literal, es un resumen denso por fecha, mismo criterio
> que `docs/planning/agencia-de-viajes/historial-archivo.md`.
>
> También absorbe el contenido de 3 archivos que vivían sueltos en esta
> carpeta y cubrían la misma pieza de trabajo (un gap real: el panel no
> tenía forma de elegir `giro` al crear un tenant, ni de editar uno ya
> creado, ni de resetear el password de su admin) — todos apuntando al
> mismo cierre real del 15/16-ago-2026: `PEGAR-EN-CLAUDE-CODE-giro-selector.md.md`
> (brief de implementación), `gap-selector-giro-tenant.md` (evidencia del
> gap + registro de despliegue a producción) y `sesion-giro-selector-panel.md`
> (copia casi idéntica del brief, con una referencia de nombre de archivo
> distinta). Los 3 se borraron del repo tras este archivado — esto es la
> única referencia que queda de esos 3 documentos.
>
> No hace falta leer esto para trabajar en el panel superadmin hoy — para
> eso, ver `CLAUDE.md` (raíz del repo, sección "Panel Superadmin") y el
> stub corto que quedó en `plan-panel-superadmin.md`. Esto es solo para
> auditoría o memoria histórica de cómo se construyó cada fase.

| Fecha | Cambio |
|---|---|
| 20-jul-2026 | **Fase 0 (Cimientos) cerrada.** Migraciones `central_users`/`central_roles`/`platform_settings`/`central_audit_logs`, guard de auth `central` separado del guard `api` de tenants, servicio `AuditLogger` inyectable, seeder del primer `central_user`. Nota de seguridad real confirmada durante la implementación: un guard/provider distinto no bastaba para aislar el panel, porque `JWT_SECRET` es compartido entre guards — un token de tenant con el mismo `id` numérico que un `CentralUser` podía autenticar por accidente contra `central`. Cerrado con `CentralUser::getJWTCustomClaims() → ['guard' => 'central']` + middleware `EnsureTokenIsCentralGuard`, implementado ya en esta fase (no diferido). |
| 20-jul-2026 | **Fase A (wrapper HTTP del provisioning) cerrada.** `TenantProvisioningService` extraído de `ProvisionTenant` (Command CLI y Controller HTTP comparten la misma clase, nunca lógica duplicada). `POST/GET central/tenants`, `GET central/tenants/{id}`, toda acción auditada. |
| 20-jul-2026 | **Fase B (B.1/B.2/B.3 — Company + SunatConfig + certificado) cerrada.** Hallazgo crítico real al iniciar: `GreenterService::getSee()` no usaba `SunatConfig` para RUC/usuario SOL/clave SOL — salían de `env('RUC')`/`env('USER_SOL')`/`env('USER_PASS')`, únicos y globales para TODA la instancia (todo tenant facturaba bajo la misma identidad SUNAT). **B.1**: conectada `SunatConfig` de verdad, `.env` limpiado sin fallback (sin fila activa → 422 explícito), gate `modo=produccion` exige certificado válido; `sandbox`/`umbo` migrados a credenciales explícitas (RUC `20161515648`, SOL `MODDATOS`/`MODDATOS`, modo `beta`) — verificado con una emisión real aceptada por SUNAT BETA. **B.2**: disco `private` nuevo (`storage/app/private`, sin symlink público) para certificados — bug real encontrado y corregido antes de cerrar: el disco no estaba listado en `config/tenancy.php filesystem.disks`, así que no quedaba aislado por tenant (confirmado con una prueba cruzada explícita ANTES de dar la fase por cerrada, no después). **B.3**: `TenantSunatController` (Company/SunatConfig/certificado), 3 hallazgos reales — conversión `.pfx`→PEM combinado vía `openssl_pkcs12_read()` (Greenter exige PEM sin cifrar, no el `.pfx` crudo); `Tenant::run()` (vendor) no tiene try/finally, mitigado sin lanzar excepciones dentro del closure; bug real encontrado por la propia prueba (no lectura de código) — `company()` serializaba el modelo FUERA de `$tenant->run()`, disparando una consulta perezosa sobre una conexión tenant ya revertida. Verificado con 8 pasos reales contra `sandbox`/`umbo`, incluida subida de un `.pfx` autofirmado con aislamiento cruzado confirmado por tenant. |
| 20-jul-2026 | **Fase B.0.5 (consolidación de la conexión `central`) cerrada.** El diagnóstico previo reveló que el grupo real de tablas a mover era de **13, no 9** (4 adicionales sin modelo Eloquent: `system_modules`/`system_features`/`system_media`/`plans`, dependientes de `systems` por FK real en Postgres). Migradas de `sv_facturacion` a `db_tenant_central` vía `pg_dump -Fc` + `pg_restore` (no migraciones nuevas escritas a mano — trae el esquema real tal cual). Bug real en el restore: un trigger hecho a mano en `detraction_codes` (función Postgres definida directo en la base, no vía migración) no se restauró solo — `pg_dump -t` no arrastra funciones sueltas; recreado manualmente, verificado funcionando con un `UPDATE` real (`updated_at` cambió al momento exacto). Auditoría posterior confirmó que era el único objeto "hecho a mano" en toda la base — sin más deuda técnica de este tipo escondida. `sv_facturacion` quedó solo con sus 30 tablas legacy pre-multitenancy. |
| 20-jul-2026 | **Fase B.2 (Suscripciones y pagos, B.2.1-B.2.5) cerrada.** `tenant_plans`/`tenant_subscriptions`/`tenant_invoices`/`tenant_payment_vouchers` (central, FK real a `tenants.id`, posible recién desde B.0.5). `TenantSubscriptionMiddleware` separado de `archivado` (sí aplica a `/auth/*` y `/portal/*`, no a las 5 rutas de PDF con URL firmada). `TenantInvoiceService` idempotente (`UNIQUE(tenant_subscription_id, periodo)`) + cron diario. `TenantOverduePaymentService` con 3 checkpoints (recordatorio → aviso a mitad de gracia → suspensión), primeros Mailables del proyecto (`MAIL_MAILER=log`, no entrega real todavía). **Bug real de Carbon 3**: `diffInDays()` ya no es absoluto por defecto (cambia de signo según dirección cronológica) — sin `absolute: true`, ningún checkpoint de gracia/suspensión llegaba a dispararse nunca, silencioso (detectado en la propia verificación, no antes). **B.2.5**: endpoints de gestión manual (mark-paid, vouchers, verify/reject, suspend/reactivate, toggle-automatic) — reactivación automática si un pago salda toda la deuda vencida, verificada con 3 pagos escalonados reales. **Bug real bloqueante encontrado recién acá**: `CentralUser` extendía `Model` en vez de `Authenticatable` — el login central **nunca había funcionado vía HTTP real** desde que existe el panel (B.2.1-B.2.4 solo se habían verificado con tinker); corregido antes de continuar, no pospuesto. Verificado con 7 casos vía HTTP real con login central real. |
| 20/21-jul-2026 | **Fase C (Backups, C.1-C.4) cerrada.** Backup manual bajo demanda (`pg_dump -Fc` individual por tenant, nunca `pg_dumpall`). Automático diario (`->dailyAt('02:00')`) con retención de 30 días — poda solo `tipo=automatico`, nunca `manual` (acción deliberada de un `central_user`). Restauración con fricción intencional: preview con token expirable (10 min) → backup de seguridad obligatorio que GATEA la restauración real → `pg_restore --clean --if-exists --single-transaction` **in-place** (nunca `DROP DATABASE`) — 2 pruebas reales de atomicidad contra Postgres 17.7 ANTES de escribir código (fallo de constraint SQL a mitad de 363 entradas TOC; `kill -9` real del proceso, 4 corridas, 2 atrapadas en `idle in transaction`) confirmaron reversión completa sin estado intermedio. Verificación de integridad automática al crear (backup "exitoso" pero corrupto se trata como fallido) + bajo demanda. **2 bugs reales de infraestructura Windows, documentados como memoria de proyecto** (riesgo genérico, no exclusivo de backups): `Process::run()` invocado desde una request HTTP (no CLI) pierde `SystemRoot` del entorno, rompiendo la resolución DNS de `pg_dump.exe`; el mensaje de error de `pg_dump.exe` en Windows viene en la codepage de consola, no UTF-8 — guardarlo crudo rompía el propio `UPDATE` de registro del fallo, escondiendo el error real detrás de otro. Verificado con evidencia real contra `sandbox` en los 3 escenarios (falla backup de seguridad → restauración nunca arranca; falla el restore real → datos intactos; restauración completa exitosa), 9 audit logs confirmados. |
| 21-jul-2026 | **Fase D (UI del panel, Pasos 0-5) cerrada en su alcance actual.** Proyecto Vue propio `central-panel/` (Vite+Vue3+TS, Bootstrap 5 vanilla, cero imports cruzados con `admin-start-kit`) — hallazgo real que simplificó el diseño: `Route::prefix('central')` no lleva middleware `tenant`, así que no hizo falta hostname dedicado ni cambios de CORS. **Paso 1** (listado): reveló **4 tenants reales, no 2** — `sandbox`/`umbo` más `negocio2` (activo) y `umbo-archivado` (remanente de la migración pre-multitenant de Umbo). **Paso 2** (detalle, 6 tabs: Company/Suscripción/Backups/SunatConfig/Certificado/Test-emission): 2 gaps de backend cerrados en el camino, ambos aditivos/solo-lectura (`GET tenants/{id}/company` nuevo, eager-load de `vouchers` en `TenantSubscriptionController::show()`) — el segundo verificado con un ciclo de escritura completo (subir + verificar voucher real). **Paso 3** (Audit Logs): `CentralAuditLogController` nuevo — 25 acciones reales confirmadas por grep de `auditLogger->log(`, más de las conocidas hasta entonces. **Paso 4** (alta de tenant por UI): gap señalado por el usuario — el endpoint existía desde Fase A pero nunca tuvo formulario, los 4 tenants reales se habían creado todos por CLI/tinker; cero cambios de backend. **Paso 5**: flujo completo crear→Company→SunatConfig→certificado→test-emission probado de punta a punta por primera vez (10 llamadas encadenadas sobre el mismo tenant, certificado `.pfx` autofirmado real) + botones Archivar/Restaurar (wrappean los comandos CLI existentes, política "archivado no borrado" por retención legal SUNAT) + Eliminar (deliberadamente estrecho, solo si el tenant nunca tuvo `Company`/`SunatConfig`/clientes/productos/ventas). **Bug real encontrado probando el propio método**: todo tenant nuevo trae 1 producto placeholder sembrado por migración (`ADELANTO-001`) que, sin excluirlo por SKU, bloqueaba el borrado de cualquier tenant, siempre — corregido antes de cerrar la fase. |
| 20/21-jul-2026 | **Fase E (verificación de emisión antes de producción, Pasos 0-2) cerrada en su alcance actual.** **Paso 0** (auditoría, solo diagnóstico): confirmó con evidencia real que los 2 únicos tenants existentes en ese momento (`sandbox`/`umbo`) emitían con el certificado demo público de Greenter (`certificado_path=null` en ambos) sin ningún indicador — y que `enviarSunat()` quemaba un correlativo SUNAT real aunque faltara `Company`/`SunatConfig`, además de devolver siempre HTTP 200 aunque el error fuera un 422 real. **Paso 1**: `Company`/`SunatConfig`+certificado ahora se validan ANTES de `reservarCorrelativo()`, código HTTP real llega a la respuesta — 30/30 tests verdes. Hallazgo downstream señalado y NO corregido (no bloqueante): `admin-start-kit/src/views/sale/index.vue` y `views/advances/show.vue` no leen bien el shape anidado de error que ahora puede devolver el catch de `getInvoice()`/`send()` en fallos de red/construcción (no en el rechazo normal de SUNAT por regla de negocio, que sigue devolviendo 200 sin cambios) — cae al mensaje genérico. **Paso 2**: `POST tenants/{id}/test-emission` (informativo, nunca reserva correlativo ni toca SUNAT, auditado siempre éxito y fallo) — conectividad real contra el WSDL de SUNAT (punto 4 del diseño original) investigada y descartada a propósito: Greenter solo trae la URL de consulta para producción, sin equivalente beta (único modo real hoy), y una mala interpretación de la respuesta arriesgaba un falso positivo/negativo en una herramienta que un superadmin tomaría como fuente de verdad. 34/34 tests verdes + verificado con tinker contra un tenant real descartable, creado y destruido en la misma sesión. |
| 22-jul-2026 | **B.2.6 cerrada** (documentado en la memoria de proyecto `project_panel_superadmin_estado.md`, nunca volcado de vuelta a `plan-panel-superadmin.md` — por eso el documento original seguía diciendo "pospuesta" pese a estar ya resuelta un día después de su última actualización). CRUD de planes (`TenantPlanController`) + crear/cambiar la suscripción de un tenant desde el panel. `umbo` (tenant real) sigue sin plan asignado — dato de negocio, no bug. |
| 15/16-ago-2026 | **Gap cerrado — gestión completa de un tenant desde el panel (giro, edición, reset de password del admin).** Origen: 3 documentos que vivían sueltos en esta carpeta y se archivaron junto con este (`PEGAR-EN-CLAUDE-CODE-giro-selector.md.md`, `gap-selector-giro-tenant.md`, `sesion-giro-selector-panel.md` — los 2 últimos casi idénticos entre sí). El pedido original era solo "selector de giro al crear" — se amplió el mismo día porque el usuario confirmó que todo el ciclo de vida de un tenant debe manejarse desde el panel, sin CLI/SSH/tinker manual. Motivo concreto: `market.umbosystem.com` se había creado desde el formulario del panel (confirmado por el usuario), candidato muy probable a `giro='retail'` por default de migración sin importar el negocio real — sin este fix no había forma de verlo ni corregirlo desde la UI. **Tres piezas, las tres cerradas**: (1) selector de `giro` obligatorio al crear (`TenantAdminController::store()`, lista blanca centralizada en `TenantProvisioningService::GIROS_VALIDOS` — antes vivía duplicada en `ProvisionTenant.php`); (2) editar un tenant ya creado (`TenantProvisioningService::actualizar()` nuevo — si `giro` cambia, dispara `migrarVertical()` retroactivo, idempotente; `domain` queda fuera a propósito por tocar el registro de `stancl/tenancy`; frontend exige checkbox de confirmación explícita si el giro cambia); (3) reset de password del admin de un tenant (`resetAdminPassword()`, busca el usuario `Super-Admin` dentro de `$tenant->run()`, asigna password en texto plano que el cast `'hashed'` de `User` hashea solo). `serialize()` ganó `giro`/`tipo`/`sunat_modo` (ninguno se exponía antes). **Bug real encontrado y corregido al verificar**: `User::role('Super-Admin')` (scope de Spatie) revienta con 500 porque `User` ya declara un método de instancia propio `role()` (relación legacy hacia `role_id`) que colisiona con el nombre del scope — corregido con `User::whereHas('roles', fn($q) => $q->where('name', 'Super-Admin'))`; documentado como advertencia para cualquier código futuro que filtre usuarios por rol de Spatie en este repo. Verificado en 2 niveles (tinker + HTTP real con login central real, tenant descartable creado y destruido). **Desplegado a producción el 16-ago-2026** (commit `0af82e2` en `main`, cherry-pick puntual desde `fix/cotizador-pasajeros-fechas-nombre-alternativa` — rama sin relación donde había quedado sin commitear por error varios días; detectado porque el primer intento de deploy no cambió nada visible) — confirmado visualmente en `http://admin.umbosystem.com/`. **Pendiente, sin resolver, deferido a propósito**: corregir el `giro` real de `market.umbosystem.com` — el mecanismo ya existe, pero requiere primero confirmar cuál es su giro real y pedir aprobación explícita del usuario antes de tocar un tenant con datos reales. |
