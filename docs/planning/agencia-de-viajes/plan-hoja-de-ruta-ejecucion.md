# Hoja de Ruta de Ejecución — Vertical Agencia de Viajes en Claude Code

> Este documento NO diseña nada nuevo — es la traducción del árbol de
> dependencias original (ver `historial-archivo.md`, el sub-plan de
> maestros iniciales que lo documentaba ya se archivó — todo ese árbol
> quedó construido hace semanas) a **sesiones concretas de Claude Code**,
> con checklist de avance.
> Vive en el repositorio de código (`docs/planning/`), no solo en Drive —
> es el documento que Claude Code lee al empezar cada sesión.
> Última actualización: 01-sep-2026 — reconciliado tras detectar que este
> archivo tenía cambios sin commitear que habían perdido las marcas `[x]`
> de 11e/11d/12 (códigos) frente al último commit (`0fb28ac`). Filas 11r,
> 11s, 11u, guardia tributario y 11v están en `origin/main` (ver nota (f)
> en §3). Fila 11w (facturación externa por tenant) cerrada 20-ago-2026,
> **sin pushear ni mergear a `main` todavía** — pendiente que el usuario
> confirme antes (ver nota (h) en §3, sigue sin resolver). 11e/11d/Módulo
> 12 (códigos) cerrados y mergeados a `main` el 25/26/27-ago-2026. Se
> agregan 2 frentes nuevos, ambos con al menos una sesión lista para
> empezar hoy: bloque 12a-12h (multi-destino + mayoristas, ver
> `plan-ejecucion-multidestino-mayoristas.md`) y bloque M1-M5 (matriz de
> opciones de hotel, ver `plan-ejecucion-matriz-hoteles-cotizador.md`),
> más el fix independiente C1 (mayorista visible en PDF). Los 2 bloques
> se cruzan en un punto (el resolver de nombre de ítem) — ver notas
> cruzadas en ambos documentos de ejecución antes de tocar esa función
> desde cualquiera de los dos.
> Ver también `historial-archivo.md` para el historial detallado de
> sesiones cerradas y `sincronizacion.md` para el protocolo de por qué
> este archivo puede desactualizarse frente al repo y cómo
> resincronizarlo.

---

## 0. Cómo usar este documento

1. Antes de abrir un chat nuevo de Claude Code, revisa la tabla de la
   sección 1 — busca la primera fila con casilla `[ ]` sin marcar. Esa es
   tu próxima sesión.
2. Al terminar una sesión (con su commit hecho), marca la casilla `[x]`
   y anota la fecha/commit en la columna "Estado".
3. Nunca saltes una fila sin marcar la anterior — cada nivel depende del
   que tiene arriba (el árbol de dependencias original que explicaba esto
   ya está archivado, todas esas dependencias ya están construidas — ver
   `historial-archivo.md` si necesitás el detalle histórico de por qué).
4. Al abrir cada chat de Claude Code, dile la ruta exacta del documento y
   la sección — no le copies el documento completo. Ejemplo de primer
   mensaje de sesión (documento de ejemplo, hoy la referencia real para
   filas abiertas es `plan-modulo-cotizaciones-reservas.md` o el brief
   `PEGAR-EN-CLAUDE-CODE-*.md` correspondiente):
   > "Vamos a construir la Sesión M1 de docs/planning/agencia-de-viajes/plan-hoja-de-ruta-ejecucion.md.
   > Lee esa fila, y pega completo
   > docs/planning/agencia-de-viajes/PEGAR-EN-CLAUDE-CODE-matriz-hoteles-m1-nucleo.md."
5. Para el historial completo de sesiones ya cerradas (detalle de qué se
   construyó, hallazgos, verificaciones), ver `historial-archivo.md` —
   no hace falta leerlo para trabajar, solo para auditoría o memoria
   histórica.

---

## 1. Las sesiones, en orden estricto

| # | Sesión | Qué construir | Documento de referencia (sección exacta) | Estado |
|---|---|---|---|---|
| 0 | Infraestructura core/verticals | Separación `database/migrations/core/` vs `verticals/agencia-viajes/`, campo `giro` en `tenants`, `tenants:provision` | `arquitectura-multitenant-backend.md`, `plan-modulo-infraestructura-multitenant.md` | [x] 27-jul-2026 — `4cd3944` |
| 1 | Catálogos centrales | `proveedor_tipos`, `temporadas` (ambos con columna `giro`) | `plan-modulo-proveedores.md` §2.6 | [x] 27-jul-2026 — `7279ec8` |
| 2 | Catálogos por tenant, sin dependencias | `destinos_atractivos` (árbol 3 niveles), `servicios`, `configuracion_agencia`, `guias` | `plan-modulo-tours-catalogo.md` completo (es corto) | [x] 27-jul-2026 — `d33bc22` |
| 3 | Puente destino↔servicio + proveedores | `destino_servicio`, `proveedores`, `proveedor_tipos_config` | `plan-modulo-tours-catalogo.md` §4, `plan-modulo-proveedores.md` §2.6 | [x] 27-jul-2026 — `78296d2` |
| 4 | Proveedor × destino | `proveedor_servicios` | `plan-modulo-proveedores.md` §2.6 | [x] 27-jul-2026 — `732534c` |
| 5 | Tarifas (la parte más grande) | `proveedor_tarifas`, `guia_tarifas`, `opciones_hotel`/`opciones_hotel_tarifas` | `plan-modulo-proveedores.md` §2.6, `plan-modulo-cotizaciones-reservas.md` §2.2, §2.4, §5.3 | [x] 27-jul-2026 — `e571fb3` |
| 6 | Catálogo de tours vendibles | `paquetes_plantilla`, `tour_itinerario_items` | `plan-modulo-cotizaciones-reservas.md` §3.7, §5.1 | [x] 27-jul-2026 — `c8581b3` |
| 7 | Motor de cotización | `cotizaciones`, `cotizacion_pasajeros`, `alternativas`, `alternativa_items`, `opcion_mayorista`, `opcion_mayorista_opcionales`, `tipo_cambio_agencia` | `plan-modulo-cotizaciones-reservas.md` §3 completo | [x] 28-jul-2026 — `b1c5a70` |
| 8 | Reserva y todo lo que dispara | `reserva`, `reserva_ventas`, `reserva_pasajeros`, `reserva_items`, `reserva_item_pasajero`, `reserva_anticipos`, `cronograma_pago_proveedor`, `reglas_cancelacion` | `plan-modulo-cotizaciones-reservas.md` §4 completo | [x] 28-jul-2026 — `0a39f76` — ⚠️ schema completo (cronograma_pago_proveedor/pago_proveedor), sin controllers/rutas — ver fila 11g |
| 9 | Integración con el core de ventas | Cambios en `Sale`/`SaleDetail`/`Product`, `pago_proveedor`, `sale_detail_items`, `pasajeros_catalogo`/`pasajero_documentos` | `plan-modulo-cotizaciones-reservas.md` §6 completo | [x] 28-jul-2026 — `59278e7` — ⚠️ schema completo, sin controllers/rutas — ni de `pago_proveedor`/`cronograma_pago_proveedor` (ver 11g) NI del flujo real de facturación reserva→Sale (ver 11u, hallazgo 20-ago-2026: esta advertencia original solo mencionaba el primer hueco, no el segundo) |
| 10 | Reporte operativo + recordatorios (backend) | Vista de `reserva_items`, `tipos_recordatorio`/`recordatorios`/`recordatorio_snooze_config` | `plan-modulo-cotizaciones-reservas.md` §8, §8bis | [x] 28-jul-2026 — `28c76f7` — ⚠️ schema/catálogo completo, sin endpoint de reporte operativo (ver fila 11e) ni motor de generación automática de recordatorios (ver fila 11f) |
| 11a | API REST + formularios maestros | Controllers/rutas para `proveedores`+tarifas, `destinos_atractivos`+`servicios`, `temporadas`, `guias`+tarifas, `configuracion_agencia`; pantallas Vue/Bootstrap 5 (`admin-start-kit`) para cada uno | `plan-modulo-cotizaciones-reservas.md` §2, §5.2, §5.3, §7 | [x] 28-jul-2026 — `21d61f3` |
| 11b | Frontend — Cotizador | Layout de 3 columnas (biblioteca / lienzo día-por-día / precio en vivo), modo Local-Nacional vs. Internacional (comparador de mayoristas), pestañas de alternativas, `PriceEngineService`, tabla `cotizacion_pasaje_aereo` | `plan-modulo-cotizaciones-reservas.md` §2.5, §3, §7.1 | [x] 28-jul-2026 — `b78c111` (rama `feature/sesion-11b-cotizador`, mergeada a `main` 29-jul-2026 en `b6c31f7`) |
| 11b2 | Catálogo de Paquetes/Tours de plantilla (admin) | Controllers/rutas para `paquetes_plantilla` + `paquete_plantilla_items` + `tour_itinerario_items` + matriz `opciones_hotel`/`opciones_hotel_tarifas` (mismo motor que `opcion_mayorista`, Sesión 7b); pantalla Vue/Bootstrap 5 en `admin-start-kit` (listado, alta/edición con itinerario día-por-día e ítems incluidos) | `plan-modulo-cotizaciones-reservas.md` §3.7, `plan-modulo-tours-catalogo.md` §5 | [x] 29-jul-2026 — `00bc458` (rama `feature/sesion-11b2-paquetes-plantilla`, mergeada a `main`) |
| 11b3 | Integración: cargar alternativa desde un paquete_plantilla | Botón "Cargar desde plantilla" en `editar.vue` (cotizador) + endpoint que resuelve cada `paquete_plantilla_item` contra su `proveedor_tarifa` real | `plan-modulo-cotizaciones-reservas.md` §3.7 | [x] 01-ago-2026 — commit `5d9d152` (ver `historial-archivo.md`) |
| 11h | Rediseño del cotizador — drawer de biblioteca, tabs de día condicionales, lienzo editable, panel de precio jerárquico | Drawer deslizable, tabs de día solo con 2+ días reales, precio editable inline, botón "Duplicar alternativa" | `plan-modulo-cotizaciones-reservas.md` §7.1 | [x] 02-ago-2026 — commit `a744e70` (ver `historial-archivo.md`) |
| 11i | Descuento configurable por agencia (% o monto) | Toggle + modo independiente ítem/global | `plan-modulo-cotizaciones-reservas.md` §7.1 | [x] 03-ago-2026 — commit `1529e07` (ver `historial-archivo.md`) |
| 11j | Drawer centrado + chips fijos con ícono + nombre comercial en biblioteca | Ajustes de UX sobre 11h | `plan-modulo-cotizaciones-reservas.md` §7.1 | [x] 03-ago-2026 — commit `fc251a9` (ver `historial-archivo.md`) |
| 11k | Hoteles de `paquete_plantilla` cableados al cotizador | `origen_tipo=hotel_plantilla` nuevo, `desdePlantilla()` devuelve hoteles disponibles | `plan-modulo-cotizaciones-reservas.md` §2.4, §7.1 | [x] 04-ago-2026 — commit `73d96f3` (ver `historial-archivo.md`) |
| 11l v2 | Itinerario editable con drag&drop + filtros de biblioteca + desglose por categoría | Reemplaza 11l/`b97b3e0` | `plan-modulo-cotizaciones-reservas.md` §5.1, §7.1 | [x] 06-ago-2026 — commit `e4be4ef` (ver `historial-archivo.md`) |
| 11m | Duplicar tour/paquete + avisos de combo incompleto + red de seguridad en cotizador | `PaquetePlantillaController::duplicar()`, avisos de combo incompleto | `plan-modulo-cotizaciones-reservas.md` §3.7, §7.1 | [x] 06-ago-2026 — commit `46d41ce` (ver `historial-archivo.md`) |
| 11n | Bloquea duplicados en Incluye | `ComboValidationService::validarNoDuplicado()` | `plan-modulo-cotizaciones-reservas.md` §3.7 | [x] 11-ago-2026 — commit `f0b2a05` (ver `historial-archivo.md`) |
| 11o | Precio por pasajero real (compartido) + cama adicional en hoteles | `ComboExplosionService` devuelve modalidad/precios crudos; campos nuevos en `opciones_hotel*` | `plan-modulo-cotizaciones-reservas.md` §2.4, §7.1 | [x] 06-ago-2026 — commit `0673d17` (ver `historial-archivo.md`) |
| 11q | Ítem manual flexible (costo/pax/cantidad) + promover a proveedor | Costo/cantidad reales en ítem manual, "Promover a proveedor real" | `plan-modulo-cotizaciones-reservas.md` §3, §6.5 | [x] 12-ago-2026 — commit `863e6d2` (ver `historial-archivo.md`) |
| 11b4a | `paquete_combo` — un paquete que agrupa 2+ tours_simple (backend) | `paquetes_plantilla.tipo`, `paquete_plantilla_items.paquete_plantilla_hijo_id`, `ComboExplosionService`/`ComboValidationService` | `plan-modulo-cotizaciones-reservas.md` §3.7 | [x] 30-jul-2026 — rama `feature/sesion-11b4a-tours-paquetes-combo-backend` |
| 11b4b | Frontend — `paquete_combo` en el admin | Pantalla para armar `paquete_combo`, extiende pantallas de 11b2 | `plan-modulo-cotizaciones-reservas.md` §3.7 | [x] 30-jul-2026 — rama `feature/sesion-11b4b-tours-paquetes-combo-frontend` |
| 11c | Frontend — Aceptación de alternativa → Reserva y pasajeros | Pantallas de reserva, datos de pasajero, asignación pasajero↔servicio | `plan-modulo-cotizaciones-reservas.md` §4, §6.5, §7 | [x] 30-jul-2026 — `131b898` |
| 11r | Fix fechas Cotización↔Reserva — Fase 1 (diagnóstico + snapshot, sin reprogramar) | Comando de diagnóstico SQL de reservas existentes (consistente/ambigua/divergente/sin_fecha/requiere_revision_operativa) antes de migrar, `reserva.fecha_viaje_desde/hasta` propias (deja de leer `cotizacion` en vivo), `reserva_items.fecha_origen` (auto/manual), regla explícita documentada sobre qué campo leer | `agencia-de-viajes/PEGAR-EN-CLAUDE-CODE-fix-fechas-fase1-diagnostico-snapshot.md` | [x] 18-ago-2026 — commit `9f1ced5` (confirmado 20-ago-2026, ver nota de commits agrupados en §3) |
| 11s | Fix fechas Cotización↔Reserva — Fase 2 (reprogramación) | Endpoint `POST reservas/{id}/reprogramar` con recálculo selectivo + re-enganche a `SalidaOperativa`/cupo, columnas de auditoría simple, bloqueo de `reasignarDia()`/`moverBloque()` sobre alternativa `aceptada`. **Depende de 11r mergeada y del diagnóstico revisado por el usuario** — no empezar antes | `agencia-de-viajes/PEGAR-EN-CLAUDE-CODE-fix-fechas-fase2-reprogramacion.md` | [x] 19-ago-2026 — commit `9f1ced5` (confirmado 20-ago-2026, ver nota de commits agrupados en §3) |
| 11u | **Facturación de reserva — crear el Sale desde una reserva** | `POST reservas/{id}/facturar` (`ReservaFacturacionController::store()`), venta pendiente de cobro (no se cobra en el mismo paso), solo contado (crédito fuera de esta fase), líneas agrupadas por categoría (5 productos placeholder, `ProductoGenericoViajeSeeder`), guard anti-doble-facturación, `reserva_ventas` + `sale_detail_items` reales. Más 3 fixes chicos hechos en la misma sesión (resumen con fecha por ítem, buscador de proveedor con tipo_tarifa/modalidad/precio, `DELETE` de ítems/pasajeros con guards). Alcance mínimo viable: un solo responsable de pago, un solo `Sale`, sin aplicar anticipos automáticamente, bloquea (no implementa) el caso de reserva ya facturada. Guardia de tratamiento tributario mixto (`destino_tributario` distinto entre ítems de una misma reserva) **cerrado 20-ago-2026 (c)** — `GET preparar-factura` (nuevo, no existía) + `POST facturar` bloquean con mensaje claro si hay mezcla, verificado con datos reales contra `DKM-2026-001` (7 ítems, mezcla real detectada). Ítems sin `destino_tributario` propio (mayorista/pasaje_aereo/manual/guía) se tratan como "nacional" por defecto (conservador). 8 tests nuevos, 169 en verde. | `agencia-de-viajes/PEGAR-EN-CLAUDE-CODE-facturar-reserva.md` | [x] 20-ago-2026 — commit `9f1ced5` (confirmado 20-ago-2026, ver nota de commits agrupados en §3) |
| 11v | **Facturación múltiple por grupo de pasajeros + separación Facturar/Facturación especial** | `POST reservas/{id}/facturar` soporta N `reserva_ventas`/`Sale` por reserva, cada uno con su propio `client_id` (persona o empresa) y texto personalizado, guardia tributario reevaluado por sub-grupo. Selección explícita de ítems sin vincular a pasajero (hallazgo real: la vinculación `reserva_item_pasajero` casi no se usa — 11/37 ítems sin asignar en el tenant, 0/8 en la reserva de prueba). Separado en dos botones tras decisión de UX con el usuario: **"Facturar"** (camino simple — todos los pasajeros pendientes, cliente de la cotización, sin selección manual; si detecta mezcla tributaria que no puede resolver solo, deriva a especial) y **"Facturación especial"** (el flujo completo de arriba — selección manual, cliente/texto por comprobante — designado como el lugar donde se agregarán futuros casos complejos: los 2 caminos de §4.3, etc.), mismo backend sin duplicar lógica. Fix colateral: modal fallaba al abrir en reservas sin ítems auto-vinculados (casi todas) | `plan-modulo-cotizaciones-reservas.md` §4.4, `agencia-de-viajes/PEGAR-EN-CLAUDE-CODE-facturar-reserva-grupo-multiples-pagadores.md` | [x] 20-ago-2026 — commit `1f908d9` sobre `9f1ced5`, rama `fix/cotizador-pasajeros-fechas-nombre-alternativa` (sin pushear) |
| 11w | **Facturación externa por tenant + por reserva (tenants sin módulo de facturación electrónica en esta plataforma)** | `tenants.facturacion_habilitada` (central, sin default, selector obligatorio en el wizard del panel superadmin) + `reserva.facturacion_externa`/`referencia_externa`/`fecha_facturacion_externa`. Gatea el botón "Facturar"/endpoints de 11u/11v sin afectar nunca el historial de `Sale` ya emitidos (eso es core, corre siempre). Reversible en ambas direcciones (agencia que empieza solo operativa y migra a facturar con nosotros, o al revés). Backfill en 3 grupos (historial de `Sale` → `true`; `giro≠agencia_viajes` sin historial → `true`; `agencia_viajes` sin historial → decisión manual, el usuario eligió `true` para `agencia-demo`). Verificado E2E con navegador real contra `agencia-demo`. 190 tests backend en verde (16 nuevos). Sesión independiente, no se mezcla con 11u/11v | `agencia-de-viajes/PEGAR-EN-CLAUDE-CODE-facturacion-externa-tenant.md`, ver también `plan-modulo-planes-acceso.md` §1 (módulo "facturador" ya anticipado, nunca construido) | [x] 20-ago-2026 — commit `4881774`, rama `feature/sesion-11w-facturacion-externa-tenant` (**sin pushear ni mergear a `main` todavía** — pendiente confirmación del usuario) |
| 11e | Reporte operativo — backend real | Endpoint que resuelve la vista de `reserva_items` con filtros (fecha, pendiente de asignar — incluyendo `es_referencial`), reemplaza lógica hoy embebida en `reservas/detalle.vue` | `plan-modulo-cotizaciones-reservas.md` §8 | [x] 25-ago-2026 — rama `feature/sesion-11e-reporte-operativo-backend` |
| 11f | Motor de generación de recordatorios | Job/Command que recorre `tipos_recordatorio` y genera filas en `recordatorios` según cada disparador | `plan-modulo-cotizaciones-reservas.md` §8bis | [ ] |
| 11g | Controllers/rutas de pago a proveedor | API REST para `pago_proveedor`/`cronograma_pago_proveedor` | `plan-modulo-cotizaciones-reservas.md` §4.6 | [ ] — **superado en diseño** por `ObligacionProveedor`/`CuotaProveedor`/`PagoProveedor`/`AplicacionPagoProveedor` (`auditoria-arquitectonica-agencia-viajes.md` §12), pendiente de ejecución fuera del alcance actual de `plan-ejecucion-multidestino-mayoristas.md` |
| 11d | Frontend — Reporte operativo (pantalla) | Vista del reporte por fecha con acciones inline (check-in, reasignar guía/proveedor), export PDF/Excel | `plan-modulo-cotizaciones-reservas.md` §8, §7 | [x] 26-ago-2026 — rama `feature/sesion-11d-reporte-operativo-pantalla`, commit `c956697`, mergeada a `main` en `d763e7b` |
| 11t | Bug — `VentaDirectaController::store()` pierde `costo_snapshot` en ítems manuales | Hallazgo colateral de la sesión 11r (no relacionado a fechas): al reenviar el payload a `crearItemManual()` para `origen_tipo='manual'`, `costo_snapshot` no llega — bug de reenvío entre validadores. Documentado en el test correspondiente de 11r, no corregido (fuera de alcance de ese brief) | Sin brief propio todavía — diagnosticar el reenvío exacto entre `VentaDirectaController::store()` y `AlternativaItemController::crearItemManual()` (o el método real, confirmar nombre) antes de escribir uno | [ ] |
| 12 | Módulo 12 — Códigos y numeración (tour/paquete/cotización/reserva/venta directa) | `configuracion_codigos`/`codigo_secuencias`/`sigla_comercial`/`reserva.codigo`/`cotizaciones.reservas_generadas`, `CodigoGeneradorService` (mismo patrón de lock que `SerieComprobanteService`), `ConfiguracionCodigosController`, pantalla `configuracion/codigos.vue` con vista previa en vivo, quita el prefijo manual de `cotizador/nueva.vue` | `plan-modulo-codigos-numeracion.md` completo (diseño aprobado 26-ago-2026 §11/§12) | [x] 26-ago-2026 — rama `feature/modulo-12-codigos-numeracion`, mergeada a `origin/main` 27-ago-2026 |
| 12a | **Multi-destino — gaps de bajo riesgo (Fase 0)** | Guard de congelamiento en `AlternativaController::update()` para descuento global sobre alternativa `aceptada`; índice único parcial `(alternativa_id) WHERE estado='elegida'` en `opcion_mayorista`. Sin dependencias — puede correr ya | `agencia-de-viajes/auditoria-arquitectonica-agencia-viajes.md` §3.2/§3.4/§19 Fase 0, `agencia-de-viajes/plan-ejecucion-multidestino-mayoristas.md`, `agencia-de-viajes/PEGAR-EN-CLAUDE-CODE-fase0-gaps-mayoristas-multidestino.md` (corregido 01-sep-2026 — la versión original marcaba `opcion_hotel_tarifa_id`/`paquete_plantilla_id` como deprecadas, chocaba con `plan-matriz-hoteles-cotizador.md`, ver nota en el brief) | [x] 01-sep-2026 — commit `3d78656`, **mergeado a `origin/main`** (fast-forward, `bdb28a8..0547123` junto con 12b). Query de verificación previa al índice corrida contra `agencia-demo` (único tenant con el vertical): **0 casos** de `alternativa_id` con más de una `OpcionMayorista` `elegida` (1 sola fila `elegida` en total) — índice creado sin necesidad de corregir datos. 6 tests nuevos, 365/365 en la suite backend |
| 12b | Multi-destino — crear `alternativa_destinos` (Fase 1) | Migración + modelo `AlternativaDestino` (`alternativa_id`, `destino_atractivo_id`, `orden`, `fecha_inicio/fin`, sin moneda/TC), backfill 1 destino por alternativa existente. Tabla aditiva, no toca `AlternativaItem`/`OpcionMayorista` todavía. Depende de 12a | `auditoria-arquitectonica-agencia-viajes.md` §7, `plan-ejecucion-multidestino-mayoristas.md` §2, `agencia-de-viajes/PEGAR-EN-CLAUDE-CODE-12b-crear-alternativa-destinos.md` (brief redactado al empezar esta sesión — corrige un gap real de la auditoría: `destino_atractivo_id` quedó nullable + `destino_texto` de respaldo, ver §0 del brief) | [x] 01-sep-2026 — commit `0547123`, **mergeado a `origin/main`** (fast-forward, `bdb28a8..0547123` junto con 12a). Backfill corrido contra `agencia-demo`: **10/10 alternativas, 0 `destino_atractivo_id` null** — incluye el caso real "Alto mayo"/"Alto Mayo" resuelto por el match case-insensitive. 6 tests nuevos, 371/371 en la suite backend |
| 12c | Multi-destino — `AlternativaItem.alternativa_destino_id` (Fase 2a) | `alternativa_destino_id` en `alternativa_items`, `dia_referencial` reiniciado por destino (decisión cerrada 01-sep-2026 — no numeración corrida), doble escritura de compatibilidad. Depende de 12b | `auditoria-arquitectonica-agencia-viajes.md` §7/§17, `plan-ejecucion-multidestino-mayoristas.md` §2, `agencia-de-viajes/PEGAR-EN-CLAUDE-CODE-12c-alternativa-item-destino.md` (brief redactado al empezar esta sesión — corrige gap real: `store()`/`duplicar()` no creaban filas en `alternativa_destinos`, ver §0 del brief) | [x] 01-sep-2026 — commit `b15db69`, mergeado a `origin/main`. `dia_referencial` NO se recalcula en esta sesión (toda alternativa tiene hoy exactamente 1 destino, no hay nada que reiniciar — queda para 12f cuando exista un 2do destino real). Backfill de ítems existentes contra `agencia-demo`: **58/58 ítems, 0 sin destino**. `AlternativaController::store()`/`duplicar()` cierran el gap (toda alternativa nueva nace con 1 `AlternativaDestino`; duplicar clona destinos + remapea `alternativa_destino_id` de los ítems, nunca apunta al original). 6 tests nuevos, 377/377 en la suite backend |
| 12d | Multi-destino — `OpcionMayorista.alternativa_destino_id` (Fase 2b) | `opcion_mayorista.alternativa_destino_id` reemplaza `alternativa_id`, índice único parcial por destino (reemplaza al de 12a). Depende de 12b, independiente de 12c | `auditoria-arquitectonica-agencia-viajes.md` §7/§9, `plan-ejecucion-multidestino-mayoristas.md` §2, `agencia-de-viajes/PEGAR-EN-CLAUDE-CODE-12d-opcion-mayorista-destino.md` | [x] 01-sep-2026 — commit `<pendiente>`, mergeado a `origin/main`. `alternativa_id` NO se dropeó (queda "en modo lectura de compatibilidad" hasta 12g, mismo criterio que las columnas deprecadas de 12a); el índice de 12a tampoco se dropeó, coexiste con el nuevo. Verificación previa al índice nuevo contra `agencia-demo`: **0 duplicados** de `elegida` por `alternativa_destino_id` (3 filas `opcion_mayorista` en total, 3/3 backfilleadas). `store()`/`duplicar()` actualizados para setear `alternativa_id`+`alternativa_destino_id` siempre juntos (nunca por separado, riesgo de desincronización de §20 de la auditoría). 4 tests nuevos, 381/381 en la suite backend |
| 12e | `contenido_tour` | Tabla nueva descriptiva sin precio (§9.1), FK opcional en `OpcionMayorista`/`OpcionMayoristaOpcional` **con snapshot al vincular** (no referencia viva — evita reescribir PDFs viejos, hallazgo §23.1.8), scoping de tenant, buscador anti-duplicados. Depende de 12d (recomendado, no estructural) | `auditoria-arquitectonica-agencia-viajes.md` §9.1/§23.1, `plan-ejecucion-multidestino-mayoristas.md` §2 | [ ] |
| 12f | Multi-destino — UI del cotizador (Fase 3) | Chips de destino sobre pestañas de día, comparador de mayoristas por destino activo, subtotal por destino, botón "+ Agregar destino", PDF agrupado por destino. La de mayor riesgo de adopción (capacitación de ventas). Depende de 12c+12d+12e | `auditoria-arquitectonica-agencia-viajes.md` §7.1/§20, `plan-ejecucion-multidestino-mayoristas.md` §2 (probablemente se divide en 2-3 briefs al llegar) | [ ] |
| 12g | Multi-destino — limpieza final (Fase 5) | Dropear columnas deprecadas de 12a, dropear `opcion_mayorista.alternativa_id`. Depende de 12f estable en producción un ciclo completo de reservas | `auditoria-arquitectonica-agencia-viajes.md` §19 Fase 5, `plan-ejecucion-multidestino-mayoristas.md` §2 | [ ] |
| 12h | Brief listo — reasignación en vivo de `OpcionMayorista` en `ReservaItem` | No bloquea 12a-12g. Hoy `ReservaItem` reasigna proveedor/guía en vivo pero no mayorista si el elegido falla tras aceptar (hallazgo §23.1.4). Confirmado como caso real (01-sep-2026); diseño de datos y UI cerrado en §9.2 + mockup `ReasignarMayorista.dc.html`; brief completo listo para pegar en Claude Code | `auditoria-arquitectonica-agencia-viajes.md` §9.2, §23.1.4, `PEGAR-EN-CLAUDE-CODE-reasignar-mayorista-vivo.md` | [ ] |
| C1 | Fix — PDF comercial revela razón social del mayorista al cliente | `AlternativaController::resolverNombreItemPdf()` cae a `Proveedor.razon_social` cuando no hay mejor dato — corregido con columna `opcion_mayorista.descripcion_publica`, sin ningún fallback al proveedor. Bug real confirmado en código (línea 610), no solo en diseño. Sin dependencias — fix chico y autocontenido, se puede ejecutar en cualquier momento | `auditoria-arquitectonica-agencia-viajes.md` §9.3, `plan-refactor-mayoristas-tramos.md` (diagnóstico original), `PEGAR-EN-CLAUDE-CODE-fix-leak-mayorista-pdf.md` | [ ] |
| M1 | Matriz de opciones de hotel — núcleo (agrupador + guard + precio en vivo) | `alternativa_items.grupo_opcion_id`/`opcion_elegida`, guard 422 al aceptar con grupo sin resolver, precio "desde $X" mientras el grupo esté abierto, reparto de `descuento_global_pct` excluye grupos abiertos. Backend puro, sin UI. Sin dependencias — puede correr ya | `plan-matriz-hoteles-cotizador.md` (diseño CERRADO, 6 rondas), `plan-ejecucion-matriz-hoteles-cotizador.md`, `PEGAR-EN-CLAUDE-CODE-matriz-hoteles-m1-nucleo.md` | [ ] |
| M2 | Matriz de hoteles — trazabilidad hacia la reserva | Fix del gap heredado (creación de ítem desde `HabitacionMatrixPicker` nunca escribía `alternativa_items.opcion_hotel_tarifa_id`); `reserva_items.opcion_hotel_tarifa_id` espejo; resolver de nombre centralizado con parámetro de audiencia (`cliente`/`interno` — coordinar con el fix C1, que ya toca el mismo resolver); fixes de `itemSinAsignacionOperativa()`/`filtrosDisponibles()`. Depende de M1 | `plan-matriz-hoteles-cotizador.md` Ronda 5, `plan-ejecucion-matriz-hoteles-cotizador.md` | Pendiente de brief |
| M3 | Matriz de hoteles — ad-hoc local + tributario | Endpoint de alta de hotel no registrado desde la pestaña Local (espejo del ya existente en Internacional), promoción de matriz completa a Proveedor real, `opciones_hotel_tarifas.tip_afe_igv`/`destino_tributario`. Depende de M1 (recomendado, no estructural) | `plan-matriz-hoteles-cotizador.md` Ronda 4/6, `plan-ejecucion-matriz-hoteles-cotizador.md` | Pendiente de brief |
| M4 | Matriz de hoteles — frontend (picker, lienzo, atajo local) | `HabitacionMatrixPicker.vue` con selección múltiple + selector tributario, pestaña Local gana el atajo ad-hoc, tarjeta colapsable de grupo en el lienzo. Depende de M2 + M3 | `plan-matriz-hoteles-cotizador.md` Ronda 3, `plan-ejecucion-matriz-hoteles-cotizador.md` | Pendiente de brief |
| M5 | Matriz de hoteles — PDF | Sección "Opciones de hoteles" en `alternativa.blade.php`, agrupada por `grupo_opcion_id`, mismo formato que los 3 documentos reales que originaron el plan. Depende de M2 (estructural), M4 (recomendado) | `plan-matriz-hoteles-cotizador.md` Ronda 3/P10, `plan-ejecucion-matriz-hoteles-cotizador.md` | Pendiente de brief |

**Regla de oro:** una sesión = un chat de Claude Code = un commit (o varios
commits pequeños dentro de la misma rama, ver sección 2). No mezcles dos
filas en un mismo chat aunque parezcan rápidas — el costo de contexto
acumulado sale más caro que abrir un chat nuevo.

**Próximo paso real (01-sep-2026):** de las filas antiguas solo quedan
abiertas 11f (recordatorios), 11g (pago a proveedor — superado en
diseño por `auditoria-arquitectonica-agencia-viajes.md` §12,
`ObligacionProveedor`/`CuotaProveedor`/`PagoProveedor`/
`AplicacionPagoProveedor`; ejecutar ese diseño cuando se retome, no el
`cronograma_pago_proveedor`/`pago_proveedor` original) y 11t (bug colateral de
`VentaDirectaController`, sin brief propio). **Fila 11w** (facturación
externa por tenant) sigue commiteada localmente pero **sin pushear ni
mergear a `main`**, pendiente confirmación del usuario (ver nota (h) en
§3) — verificar si sigue siendo así antes de asumirlo.

Hay 3 frentes de trabajo nuevos, listos para arrancar hoy, **sin
bloquearse entre sí**:
- **12a-12h** (multi-destino + mayoristas + `contenido_tour`) — 12a
  tiene brief completo y corregido
  (`PEGAR-EN-CLAUDE-CODE-fase0-gaps-mayoristas-multidestino.md`), 12h
  también (`PEGAR-EN-CLAUDE-CODE-reasignar-mayorista-vivo.md`, pero
  **antes de pegarlo confirmar que se agregó `opcion_mayorista_id` a la
  migración de §1** — el brief original asumía que ya existía en
  `reserva_items` y no es cierto, ver la corrección fechada dentro del
  archivo). 12b-12g se redactan al llegar a cada una.
- **M1-M5** (matriz de opciones de hotel) — M1 tiene brief completo
  (`PEGAR-EN-CLAUDE-CODE-matriz-hoteles-m1-nucleo.md`), backend puro,
  sin dependencias. M2-M5 se redactan al llegar a cada una.
- **C1** (PDF revela mayorista al cliente) — brief completo y corregido
  (`PEGAR-EN-CLAUDE-CODE-fix-leak-mayorista-pdf.md`), fix chico
  autocontenido, se puede intercalar en cualquier momento.

**Punto de cruce a vigilar:** C1 y M2 tocan el mismo resolver de nombre
de ítem (`resolverNombreItemPdf()`/`resolverNombreItem()`) — conviene
que C1 se mergee antes de que M2 lo centralice, para no tocarlo dos
veces (ver nota en `plan-ejecucion-matriz-hoteles-cotizador.md`). Fuera
de eso, los 3 frentes son independientes — el usuario decide el orden
según prioridad de negocio.

---

## 2. Vínculo con Git

- Una rama por sesión: `feature/sesion-0-infraestructura`,
  `feature/sesion-1-catalogos-centrales`, etc.
- Al terminar la sesión y verificar que corre, merge a `main` (o la rama
  base del proyecto) y **recién ahí** se marca `[x]` en la tabla de
  arriba.
- El mensaje de commit referencia la sesión y el documento fuente, ej.:
  `feat(sesion-1): catálogo central proveedor_tipos y temporadas (plan-modulo-proveedores.md §2.6)`

## 3. Últimas actualizaciones (resumen)

> Historial completo (25-jul a 12-ago-2026, todas las sesiones 0 a 11q)
> archivado en `historial-archivo.md`. Resumen: las 18 sesiones
> originales del vertical están construidas y mergeadas. El fix de
> fechas (11r/11s) y el bug colateral 11t se agregaron después. El
> 20-ago-2026 se suma 11u (facturación de reserva), el gap más
> importante encontrado hasta ahora: hoy no existe forma de facturar
> una reserva desde el sistema. 11e/11d (reporte operativo) y Módulo 12
> (códigos) cerrados y mergeados 25/26/27-ago-2026 — solo 11f
> (recordatorios), 11g (pago a proveedor) y 11t siguen abiertos de todo
> este bloque.

| Fecha | Cambio |
|---|---|
| 01-sep-2026 (reconciliación) | **Este archivo tenía cambios sin commitear que habían perdido las marcas `[x]` de 11e/11d/Módulo 12 (códigos) frente al último commit real (`0fb28ac`) — restauradas.** Se agrega la fila del Módulo 12 (códigos, cerrado 26/27-ago-2026, se había caído de la tabla). Se cierra el diseño de `plan-matriz-hoteles-cotizador.md` (6 rondas, 18 preguntas) y se agregan las filas M1-M5 con `plan-ejecucion-matriz-hoteles-cotizador.md` (M1 con brief completo). En el camino se encuentra y corrige un conflicto real entre `plan-ejecucion-multidestino-mayoristas.md` (sesión 12a planeaba deprecar `alternativa_items.opcion_hotel_tarifa_id`, que la matriz de hoteles necesita viva) — causado por una auditoría que afirmó, incorrectamente, que varios documentos del proyecto no existían. El usuario decide además que `alternativa_destinos` (no `alternativa_tramos`) es el camino para multi-destino — `plan-refactor-mayoristas-tramos.md` queda cerrado, superado. Se descubre y cierra C1 (el PDF comercial imprimía la razón social del mayorista al cliente, `resolverNombreItemPdf()` línea 610) con brief propio (`PEGAR-EN-CLAUDE-CODE-fix-leak-mayorista-pdf.md`), corrigiendo 2 errores técnicos encontrados en la primera versión de ese brief (referencia a un archivo inexistente, migración con `after()` sobre una columna que no existe). Estado al cierre: 3 frentes de trabajo listos para empezar sin bloquearse entre sí (12a-12h, M1-M5, C1) — ver "Próximo paso real" arriba. |
| 01-sep-2026 | **Se agrega el bloque 12a-12h** — plan de ejecución para multi-destino (`alternativa_destinos`), reubicación de `OpcionMayorista` y `contenido_tour`, cerrado tras la auditoría arquitectónica profunda y su segunda pasada de brechas (`auditoria-arquitectonica-agencia-viajes.md` §7, §9, §9.1, §19, §23). Decisiones cerradas con el usuario para esta ronda: una reserva por viaje completo (no por destino), `dia_referencial` reiniciado por destino, facturación partida por destino y pagos a proveedor/fidelización quedan fuera de esta ronda. Detalle de la secuencia y de qué depende de qué en `plan-ejecucion-multidestino-mayoristas.md` (documento nuevo). Sesión 12a ya tiene brief completo y puede empezar de inmediato; 12b en adelante se redactan al llegar cada una. 12h es la reasignación de mayorista en vivo si el elegido falla tras aceptar la reserva — confirmada como caso real y con diseño cerrado (§9.2), solo falta el brief de ejecución — que no bloquea nada del resto del bloque. |
| 20-ago-2026 (h) | **Fila 11w commiteada — `4881774` en rama `feature/sesion-11w-facturacion-externa-tenant`, sin pushear ni mergear a `main` todavía.** Claude Code dejó 4 archivos sueltos sin trackear a propósito (`AGENTS.md`, dos parches, un CSV) — son anteriores a esta sesión, no forman parte de esta feature, no se incluyeron en el commit. Pendiente confirmación del usuario antes de pushear/mergear (mismo criterio de cautela que dejó el enredo de ramas apiladas del bloque 11r-11v — confirmar cada vez hasta que quede claro que no se repite). |
| 20-ago-2026 (g) | **Fila 11w cerrada y verificada, sin commitear todavía.** `tenants:backfill-facturacion-habilitada` traía un gap real frente al brief: solo implementaba 2 de los 3 grupos del criterio acordado con el usuario, dejando en `NULL` cualquier tenant sin `Sale` en vez de defaultear a `true` los de `giro≠agencia_viajes` (ej. `retail`) — corregido el comando y su test antes de correr nada real. Migraciones aplicadas (`tenants.facturacion_habilitada` central + `reserva.facturacion_externa`/`referencia_externa`/`fecha_facturacion_externa` en `agencia-demo` vía `tenants:migrate-verticales`). Backfill corrido contra los tenants reales: `umbo`/`sandbox`/`umbo-archivado` → `true` (historial de `Sale` real), `negocio2` → `true` (retail, sin concepto de facturación externa), `agencia-demo` → único caso genuinamente ambiguo (agencia_viajes sin historial de `Sale` todavía) — el usuario decidió `true` (paquete completo), aplicado por el endpoint auditado real (queda en `central_audit_logs`). Verificación E2E contra `agencia-demo` con sesión real logueada: venta descartable creada (S/177), confirmado por grep que ningún otro controller lee el flag, apagado `facturacion_habilitada` y navegado de verdad a Ventas/Cuentas por Cobrar/Notas de Crédito de esa venta — 100% accesible sin cambios. Flag restaurado a `true`, datos de prueba (venta, sesión de caja, caja) eliminados al cerrar. 190 tests backend en verde (16 nuevos/actualizados), type-check frontend sin regresiones (0 en `central-panel`, mismo baseline de 45 preexistentes en `admin-start-kit`). Documentado en `CLAUDE.md`. **Nada de esto está commiteado** — pendiente que el usuario confirme antes de crear el commit. |
| 20-ago-2026 (e) | **Nota de commits agrupados — confirmado con `git show --stat 9f1ced5` (30 archivos, 4408 inserciones/73 borrados).** El commit `9f1ced5` resultó cubrir 4 filas de esta tabla a la vez, no solo una: **11r + 11s** (fix de fechas Cotización↔Reserva, diagnóstico/snapshot/reprogramación — nunca se había confirmado su commit real hasta hoy, quedaban con "pendiente de confirmar") + **11u** (facturación de reserva) + el parche de **guardia tributario**, todo junto porque nada de eso se había commiteado hasta esta sesión. `1f908d9` (encima de `9f1ced5`) es aparte: fila **11v** (facturación múltiple + separación de botones). Ambos commits viven en la rama `fix/cotizador-pasajeros-fechas-nombre-alternativa`, que además tiene **3 commits previos de sesiones anteriores sin relación** (`84c8685` fix combo/redondeo, `7ee68a3` búsqueda DNI/RUC + CRUD sucursales, `d885af6` panel-superadmin selector de giro) apilados linealmente debajo — ninguno de los 5 está en `main` ni pusheado. Se acuerda con el usuario separar la pila en 5 ramas con nombre (una por commit/sesión, ver plan de git abajo) para volver a la regla de "una rama por sesión" antes de mergear, en vez de mergear todo junto como una sola rama con 5 sesiones adentro. **Plan de ramas:** `feature/sesion-panel-superadmin-giro-tenant` (`d885af6`) → `feature/sesion-clientes-sucursales` (`7ee68a3`) → `feature/sesion-combo-redondeo` (`84c8685`) → `feature/sesion-11r-11s-11u-facturacion-guardia` (`9f1ced5`, cubre fechas+facturación básica+guardia tributario) → `feature/sesion-11v-facturacion-grupo` (`1f908d9`) — mergear a `main` en ese orden (cada rama contiene a la anterior por estar apiladas, así que el merge es limpio si se respeta el orden), luego borrar la rama vieja mezclada. **✅ EJECUTADO Y PUSHEADO, ver entrada (f) siguiente.** |
| 20-ago-2026 (f) | **Todo el trabajo de hoy queda en `origin/main` — `git push` confirmado (`a73a8b4..2f3ce7f main -> main`).** Ejecutado el plan de la nota (e): al mergear en el worktree `sistemfe-guia-item` se descubrió que `main` local y `origin/main` habían divergido (7 commits locales / 1 remoto) — el commit remoto (`a73a8b4`, 19-ago 20:22) resultó ser el mismo contenido de `7ee68a3`+`84c8685` ya pusheado por 3 agentes en background (worktrees `agent-*`, confirmados inertes/limpios sin trabajo en curso antes de tocar nada, luego removidos con `git worktree remove`). El merge final salió con diff vacío (0 archivos), confirmando que era contenido idéntico llegado por 2 caminos — verificado con la suite completa después del merge (174 tests backend en verde, type-check frontend mismo baseline de 45), no solo por ausencia de conflicto de texto. `main` en `origin` y en el worktree `sistemfe-guia-item` quedó en `2f3ce7f`. El worktree principal (`sistemfe`, la ruta real del proyecto) quedó en detached HEAD apuntando también a `2f3ce7f` — pendiente crear ahí una rama con nombre para la próxima sesión (11w es la candidata natural). Con esto, **11r, 11s, 11u, el guardia tributario y 11v dejan de ser "cerrado localmente" y pasan a ser "cerrado en `main`" de verdad** — es la primera vez en este bloque de trabajo que el estado documentado acá coincide con el estado real del repo remoto. |
| 20-ago-2026 (d) | **Fila 11v cerrada** (facturación múltiple por grupo de pasajeros + separación Facturar/Facturación especial). `POST reservas/{id}/facturar` ahora crea N `Sale` por reserva, cada uno con su propio cliente (obligatorio, ya no fijo a `cotizacion.cliente_id`) y texto personalizado opcional, cubriendo el subconjunto de pasajeros elegido; el guardia tributario (parche anterior) se reevalúa por sub-grupo, no por reserva completa. **Hallazgo real que cambió el diseño del brief:** el mecanismo original (derivar ítems automáticamente desde `reserva_item_pasajero`) no sirve con los datos reales de hoy — esa vinculación casi no se usa (11/37 ítems sin asignar en todo el tenant, 0/8 en la reserva de prueba). Resuelto con selección explícita: los ítems sin vincular se ofrecen aparte para marcarlos a mano por comprobante (decisión tomada con el usuario entre 3 opciones — bloquear hasta asignación completa hubiera dejado casi todo el tenant sin poder facturar; asignar todo lo suelto al primer Sale arriesgaba cobrarle a alguien de más). **Separación de UX decidida con el usuario a mitad de sesión** (aplica el principio ya documentado en §7 del plan — "lo avanzado se mantiene oculto hasta que el caso lo amerite"): botón **"Facturar"** (camino simple — todos los pasajeros pendientes, cliente de la cotización, sin selección manual; si detecta mezcla tributaria sin resolver, deriva explícitamente a especial) y botón **"Facturación especial"** (el flujo completo — selección de pasajeros/ítems, cliente y texto por comprobante — designado como el lugar permanente para casos complejos futuros, ej. los 2 caminos de §4.3). Mismo backend en ambos, sin duplicar lógica. Bug real encontrado probando en vivo, no en tests: el modal fallaba al abrir en cualquier reserva sin ítems auto-vinculados (o sea, casi todas) — corregido y reverificado contra la misma reserva real que había fallado. Verificado en navegador de punta a punta contra `agencia-demo` (selección de pasajeros, ítems reactivos, buscador de cliente, detección de mezcla tributaria derivando al flujo especial) sin confirmar envío real. 13 tests nuevos, 174/174 backend en verde, type-check frontend sin regresiones (mismo baseline de 45). Documentado en `plan-modulo-cotizaciones-reservas.md` y `CLAUDE.md`. |
| 20-ago-2026 (c) | **Guardia de tratamiento tributario mixto cerrado** (parche sobre 11u, `PEGAR-EN-CLAUDE-CODE-facturar-reserva-guardia-tributario.md`). `ReservaFacturacionController` detecta si una reserva mezcla `destino_tributario` distintos y bloquea — nuevo endpoint `GET preparar-factura` (el brief original de 11u lo pedía y nunca se había construido) + validación server-side real en `POST facturar` (no confía solo en el frontend). Verificado con datos reales, no solo tests: reserva real `DKM-2026-001` de `agencia-demo` (7 ítems) tenía una mezcla tributaria real — el sistema la bloqueó con mensaje claro y ocultó el botón de confirmar; al destildar el ítem conflictivo el bloqueo se levantó al instante (confirma también el caso homogéneo). Hallazgo colateral corregido de paso: al reprogramar un viaje, un ítem sin fecha calculable quedaba con su fecha vieja sin aviso — ahora aparece listado con motivo explícito. Limitación documentada a propósito: `destino_tributario` solo existe hoy para ítems `origen_tipo=proveedor` — mayorista/pasaje_aereo/manual/guía no tienen ese dato propio, se tratan como "nacional" por defecto (conservador, nunca deja pasar una mezcla real en silencio, pero puede sobre-bloquear casos que en realidad son homogéneos). 8 tests nuevos, 169/169 backend en verde, type-check frontend sin regresiones (mismo baseline de 45). **Commiteado 20-ago-2026 — `1f908d9` sobre `9f1ced5`, rama `fix/cotizador-pasajeros-fechas-nombre-alternativa` (sin pushear todavía).** Documentado en `plan-modulo-cotizaciones-reservas.md` y `CLAUDE.md` además de aquí. Justo después de esto se agrega la fila 11v (ver entrada siguiente) — el usuario reporta que la facturación múltiple por grupo de pasajeros (varios pagadores, boleta/factura mixta, texto personalizado) es un caso frecuente, no soportado por el diseño de 11u. |
| 20-ago-2026 (b) | **Fila 11u cerrada.** `ReservaFacturacionController::store()` (`POST reservas/{id}/facturar`) cierra el gap real: venta pendiente de cobro (no se cobra en el mismo paso — decidido con el usuario para acotar alcance, Cuentas por Cobrar/editar venta ya resuelven el cobro después), solo contado (crédito queda fuera de esta fase, mismo criterio de acotar que 11r/11s), líneas agrupadas por categoría (5 productos placeholder de `ProductoGenericoViajeSeeder`, corridos en `agencia-demo`), guard anti-doble-facturación. Verificado con datos reales: reserva #18 facturada (S/100, 2 líneas agrupadas), reintento de facturación confirmado bloqueado, datos revertidos al estado previo a la verificación. Además, en la misma sesión se resolvieron 3 fixes chicos que salieron en el camino: resumen de reserva ahora muestra la fecha de cada ítem (dos servicios iguales en días distintos dejaban de verse idénticos); buscador de proveedor en `reservas/detalle.vue` ahora muestra `tipo_tarifa · modalidad · precio` (mismo patrón que ya usaba el cotizador, antes ambigüo con proveedores duplicados); `DELETE reserva-items/{id}` y `DELETE reserva-pasajeros/{id}` nuevos, con guards contra reserva no activa, ítem/pasajero ya facturado, y último pasajero. 166/166 tests de backend en verde (27 nuevos), type-check de frontend sin regresiones (45 errores preexistentes, mismo baseline). **Pendiente, no incluido en esta sesión a propósito (fuera del alcance mínimo acordado):** aplicar `PEGAR-EN-CLAUDE-CODE-facturar-reserva-guardia-tributario.md` — guardia contra reservas que mezclan `destino_tributario` distinto entre sus ítems (riesgo real de comprobante SUNAT con exoneración mal calculada), encontrado en la misma conversación de diseño pero tratado a propósito como parche aparte. Los 2 caminos de §4.3 (reserva ya facturada + servicio nuevo), varios pagadores (§4.4) y aplicar `reserva_anticipos` automáticamente (§4.5) siguen fuera de alcance, sin número de sesión asignado todavía. |
| 20-ago-2026 (a) | **Se agrega la fila 11u — gap encontrado en revisión crítica del código construido (no solo lectura del plan): `reserva_ventas` y `sale_detail_items` existen como tablas/modelos desde la Sesión 9, pero ningún controller las usa.** `ReservaVenta` solo aparece como guard de bloqueo en `ReservaController::cancelar()`; `SaleDetailItem` no lo referencia nada más que su propio modelo; `SaleController` no tiene noción de `reserva_id`; `VentaDirectaController` nunca crea un `Sale` real pese al nombre. En la práctica, hoy se puede cotizar/aceptar/gestionar una reserva completa pero no hay ningún botón ni endpoint que emita el comprobante SUNAT — se hace por fuera del sistema. Se decide con el usuario: alcance mínimo viable (un solo responsable, un solo `Sale`, sin aplicar anticipos automáticamente, bloquea en vez de implementar el caso de reserva ya facturada — §4.3/§4.4 completos quedan para sesiones futuras) y prioridad **antes** que 11e/11f/11g/11d/11t. Brief completo en `PEGAR-EN-CLAUDE-CODE-facturar-reserva.md`. La advertencia ⚠️ original de la fila 9 (Sesión 9) se actualiza — solo mencionaba el hueco de `pago_proveedor`/`cronograma_pago_proveedor` (11g), no este. |
| 19-ago-2026 | **Fila 11s cerrada — cierra el fix de fechas Cotización↔Reserva completo (11r+11s).** `POST reservas/{id}/reprogramar` (`ReservaController::reprogramar()`, migración con columnas de auditoría simple `fecha_viaje_desde_original`/`fecha_viaje_hasta_original`/`fecha_reprogramacion`/`motivo_reprogramacion`): mueve `reserva.fecha_viaje_desde/hasta` y recalcula `reserva_items.fecha` SOLO para los ítems `fecha_origen='auto'` — los `'manual'` quedan intactos y vuelven en `items_no_tocados` de la respuesta. Re-engancha `SalidaOperativa` de los ítems recalculados que cambiaron de fecha (desengancha de la vieja sin borrarla — puede seguir compartida por otra reserva — y reintenta `engancharSalidaOperativa()` con las mismas reglas que al aceptar). **Hallazgo confirmado leyendo código, no asumido del brief:** el punto 5 de §1.2 del brief ("cuidado con `SalidaMayorista.cupo_ocupado`") no aplica — ese contador es por RESERVA completa (`reserva.mayorista_elegida_id`, fijado una única vez al aceptar/cancelar, atado a una salida de catálogo con fecha propia), nunca por `reserva_item` — no existe ningún camino donde recalcular `reserva_items.fecha` deba mover ese cupo, documentado en el código y en `CLAUDE.md` sin agregar lógica de más. `reasignarDia()`/`moverBloque()` (`AlternativaItemController.php`) ahora rechazan con 422 explícito si `alternativa.estado === 'aceptada'` ("usa reprogramar sobre la reserva en vez de mover ítems acá"), cerrando el hueco que dejaba abierto el bloqueo parcial de 11r. Frontend: botón "Reprogramar viaje" + modal (fecha desde/hasta + motivo obligatorio) en `reservas/detalle.vue`, badge "Fecha manual" en los ítems con `fecha_origen='manual'`, banner de `items_no_tocados` tras confirmar (mismo patrón visual que el aviso de "Sincronizar"). Verificación: 139 tests backend en verde (131 base + 4 de 11r + 8 nuevos de 11s: `ReservaReprogramarTest` —4 casos, incluye recálculo/preservación de manuales/re-enganche real a `SalidaOperativa`/reprogramar dos veces seguidas— y `AlternativaItemBloqueaMoverSiAceptadaTest` —4 casos—); type-check frontend sin regresiones (45 errores preexistentes, mismo baseline, cero nuevos); verificación E2E real contra `agencia-demo` (reserva #12, `kur-2026-001`) — reprogramada de `2026-08-27` a `2026-11-01`, ítems auto movidos, un ítem marcado manual a propósito quedó intacto y listado en `items_no_tocados`, guard de `reasignarDia()` confirmado con 422 real contra la alternativa aceptada real de esa reserva — datos revertidos a su estado exacto de antes de la verificación (mismos valores que dejó el diagnóstico de 11r) al terminar. Migración aplicada a `sistemafe_test_migrations` y al tenant real vía `tenants:migrate-verticales`. Detalle completo en `PEGAR-EN-CLAUDE-CODE-fix-fechas-fase2-reprogramacion.md` ("Estado real al cierre") y `CLAUDE.md`. |
| 18-ago-2026 (c) | **Fila 11r cerrada.** Diagnóstico (`agencia-viajes:diagnosticar-fechas-reserva`, solo lectura) corrido contra `agencia-demo` (único tenant real con el vertical): 5 reservas — 2 CONSISTENTE, 1 AMBIGUA, 1 DIVERGENTE, 1 SIN_FECHA, todas datos de prueba descartables (confirmado con el usuario, no bloqueó la migración). Migración (`reserva.fecha_viaje_desde/hasta`, `reserva_items.fecha_origen`) con backfill exacto según el algoritmo del brief, aplicada primero a `sistemafe_test_migrations` y luego al tenant real vía `tenants:migrate-verticales` — el resultado coincidió con lo predicho por el diagnóstico (reserva 16 quedó en `2026-08-30`, no en `2026-08-27` que muestra la cotización hoy). Código: `crearReservaDesdeAlternativa()` copia la fecha una sola vez; `sincronizarItems()`/`respuestaDetalle()` migrados a leer `reserva.fecha_viaje_desde`; `ReservaItemController::update()` marca `fecha_origen='manual'` cuando el request trae `fecha`; regla de qué campo leer documentada en docblocks de `Reserva`/`ReservaController` + `AGENTS.md`/`CLAUDE.md`. `VentaDirectaController` no necesitó cambios (confirmado con test). Verificación: 135 tests backend en verde (131 + 4 nuevos/extendidos), incluido el test de regresión del bug de fondo; type-check frontend sin regresiones (45 errores preexistentes, mismo baseline, cero nuevos); verificación E2E real contra `agencia-demo` — se editó la cotización de la reserva 16 en vivo y se confirmó que `cabecera.fecha_viaje_desde` de `show()` no se movió mientras que `reserva.alternativa.cotizacion.fecha_viaje_desde` sí cambió (comportamiento esperado y documentado en §3.4 del brief), después se revirtió la cotización a su valor original. No se tocó nada de reprogramación/`SalidaOperativa`/bloqueo de `reasignarDia`/`moverBloque` — queda para 11s, como indicaba el brief. **Hallazgo colateral, fuera de alcance, no arreglado:** `VentaDirectaController::store()` pierde `costo_snapshot` al reenviar el payload a `crearItemManual()` para `origen_tipo='manual'` — bug de reenvío entre validadores, ajeno a fechas, documentado en el test correspondiente en vez de corregido (fuera del alcance de este brief). Pendiente decidir con el usuario dónde loguearlo formalmente (¿`TODO.md`? ¿fila nueva en la hoja de ruta?) y cuándo priorizarlo. |
| 18-ago-2026 (b) | Revisión cruzada del diseño de 11r con una segunda IA (ChatGPT/Codex): confirma el diseño de Claude en todos los puntos estructurales, pero encuentra 2 huecos reales — (1) `RELACIONES_DETALLE` sigue exponiendo `reserva.alternativa.cotizacion.fecha_viaje_desde` en el JSON de `show()` aunque se corrija la cabecera (serialización automática de Eloquent) — se documenta como regla explícita en el brief, no se puede depender solo de la cabecera; (2) el bug ya está activo en producción, así que un backfill directo desde la cotización actual puede "oficializar" fechas ya corrompidas en reservas reales — se agrega un comando de diagnóstico SQL obligatorio (clasifica cada reserva existente en consistente/ambigua/divergente/sin_fecha/requiere_revision_operativa) **antes** de cualquier migración. Con este hallazgo, se decide con el usuario partir 11r en 2 sesiones separadas: **11r** (diagnóstico + snapshot + `fecha_origen`, cierra el bug activo con bajo riesgo) y **11s** (reprogramación, después, sobre datos ya verificados). Briefs actualizados: `PEGAR-EN-CLAUDE-CODE-fix-fechas-fase1-diagnostico-snapshot.md` y `PEGAR-EN-CLAUDE-CODE-fix-fechas-fase2-reprogramacion.md` (reemplazan el brief único anterior). |
| 18-ago-2026 (a) | Se agrega fila **11r** — bug real encontrado en conversación con el usuario: la reserva no tiene fecha propia, lee `cotizacion.fecha_viaje_desde/hasta` en vivo a través de `alternativa`, así que editar la cotización después de aceptada mueve la fecha de la reserva ya confirmada en silencio (sin auditoría, sin distinguir ítems editados a mano). Análisis de código completo hecho por una sesión de Claude Code, confirmado línea por línea. (Ver entrada (b) — el brief original de esta entrada fue reemplazado el mismo día tras revisión cruzada.) |
| 12-ago-2026 | Backfill comprimido de 76 commits/~26 merges (30-jul a 12-ago) que nunca habían llegado a este documento — reconstruido desde `git log`. Agrega filas 11h/11i/11j/11k/11l v2/11m/11n/11o/11q (todas cerradas). Detalle completo en `historial-archivo.md`. |
| 30-jul-2026 | Fila 11b4b cerrada (frontend `paquete_combo`), verificado E2E con Playwright real contra `agencia-demo`. Hallazgo real: `tenants:migrate` no corre `tenant/verticals/` — requiere el paso manual documentado en `historial-archivo.md` para cualquier sesión que agregue migraciones nuevas al vertical. |
| 30-jul-2026 | Sesión 11c pusheada (reserva/pasajeros, backend + frontend), 35 checks E2E contra `agencia-demo` dentro de una transacción revertida. |
| 18-ago-2026 | Se archiva el historial detallado (25-jul a 12-ago) en `historial-archivo.md` y se recorta este documento a la tabla de estado + últimas 3 entradas, para que las sesiones nuevas no tengan que releer meses de historial cerrado. Se crea `sincronizacion.md` con el protocolo de sync entre Claude Code (repo) y el Proyecto de claude.ai. |
