VITE_API_BASE_URL=http://umbo.sistemafe.test/api/
VITE_STORAGE_URL=http://umbo.sistemafe.test

# Solo en desarrollo: puerto de la API usado al calcular la URL dinámicamente
# a partir del hostname actual (ver src/helpers/apiBaseUrl.ts). Puerto 80 =
# el vhost de Apache (multi-hilo), no `php artisan serve` (single-thread) —
# ver CLAUDE.md, sesión 2026-08-13.
VITE_API_DEV_PORT=80