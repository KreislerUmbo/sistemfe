VITE_API_BASE_URL=http://umbo.sistemafe.test:8000/api/
VITE_STORAGE_URL=http://umbo.sistemafe.test:8000

# Solo en desarrollo: puerto de la API usado al calcular la URL dinámicamente
# a partir del hostname actual (ver src/helpers/apiBaseUrl.ts). Opcional —
# si no se define, usa 8000 por defecto. En producción se ignora: se usa
# VITE_API_BASE_URL fijo de arriba.
VITE_API_DEV_PORT=8000
